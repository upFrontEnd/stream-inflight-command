# bot/sounds/

Dépose ici les fichiers audio (`.mp3`, `.wav`...) référencés dans `bot/commands.js`.

Ce dossier n'est pas commité (voir `.gitignore`) : les sons sont souvent des
extraits protégés par droits d'auteur, et ce repo est public.
